.hero
	padding 0 0 spacing(24px) 0
	
	h2
		set-font-size(16px)
		line-height 1.6
		color var(--text-color)

		em
			color var(--text-color)
			font-family font-serif
			set-font-size(20px)
			font-style italic
			font-weight 400

.ui
	.ui-grid
		display grid
		grid-template-columns 1fr 1fr
		grid-gap 16px

		.portfolio__item
			margin-bottom 0